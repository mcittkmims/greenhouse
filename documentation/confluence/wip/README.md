# WIP — Work In Progress

Files in this folder are currently being actively edited and have not yet been pushed to Confluence.

Workflow:
1. Copy the `.gcm` file here from `documentation/confluence/cloud/` to edit it
2. When done, push with `confluence_push.py --file <filename>.gcm`
3. Move the file back to `documentation/confluence/cloud/`
